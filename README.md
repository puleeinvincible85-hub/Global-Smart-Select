# Global Smart Select

Global Smart Select is an MVP for multilingual business-data normalization.

The application will eventually extract business entities from multilingual content,
map them to a governed taxonomy, provide confidence scores, and show supporting evidence.

## Run locally

Create a virtual environment:

```powershell
python -m venv .venv